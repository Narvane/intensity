package com.intensity.participant.repository;

import com.intensity.participant.entity.AllowlistEmail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AllowlistEmailRepository extends JpaRepository<AllowlistEmail, String> {
}
