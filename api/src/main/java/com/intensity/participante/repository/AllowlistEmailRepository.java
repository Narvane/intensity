package com.intensity.participante.repository;

import com.intensity.participante.entity.AllowlistEmail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AllowlistEmailRepository extends JpaRepository<AllowlistEmail, String> {
}
