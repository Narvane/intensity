# Demo SPA — Vite `--mode demo` (public sample on the VPS).
# Domains must match deploy/.env.demo DEMO_* hosts.
VITE_API_URL=https://demo-intensity-api.narvane.com.br
VITE_INVITE_BASE_URL=https://demo-intensity.narvane.com.br/join
VITE_DEMO=true
