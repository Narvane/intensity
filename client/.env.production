# Production API URL (baked into the store build — DT-09)
# Replace with your public HTTPS API before release.
VITE_API_URL=https://api.narvane.com.br
VITE_INVITE_BASE_URL=https://app.narvane.com.br/join